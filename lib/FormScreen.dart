import 'package:flutter/material.dart';

class FormScreen extends StatelessWidget {
  static String routeName = 'form';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('The form will go here one day!'),
    );
  }
}
