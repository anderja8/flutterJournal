import 'package:flutter/material.dart';

class DetailsScreen extends StatelessWidget {
  static String routeName = 'details';

  @override
  build(BuildContext context) {
    return Scaffold(body: Text('I will add details here later'));
  }
}
